@{ ViewData["Title"] = "One-time-password gebruiken"; }
<div class="container">
    <div class="row">
        <div class="col-sm-12 align-middle">

            <h1>Bijna klaar</h1>
            <p>
                Omdat we jou maaltijdplanner wel een beetje veilig willen maken, moet je via een one-time-password app de maaltijdplanner registreren.
                Open daarom bijvoorbeeld Google Authenticator, One-password etc en scan de barcode. Deel de barcode met wie je de maaltijdplanner wil gebruiken.
                Je kan de barcode later altijd in de opties van maaltijdplanner vinden.
            </p>
        </div>
        <div class="col-sm-4 align-middle">
            <img src="@Model" />
            <form asp-controller="Home" asp-action="ValidateToken">
                <div class="input-group input-group-lg">
                    <input type="text" name="token" maxlength="6" class="form-control" placeholder="One-time-password" required autocomplete="off" />
                    <div class="input-group-append">
                        <button class="btn btn-outline-secondary" type="submit" id="button-addon2">Valideer</button>
                    </div>
                </div>
            </form>

        </div>
            @if (ViewBag.Error != null)
            {
                <div class="col-sm-12 align-middle mt-3">
                    <div class="alert alert-danger" role="alert">
                        @ViewBag.Error
                    </div>
                </div>
            }
    </div>
</div>

@section scripts {
    <script src="~/dist/main.js" asp-append-version="true"></script>
}
